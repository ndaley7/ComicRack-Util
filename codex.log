2026-08-30

Prompt:
Make a master Python UI for ComicRack utility workflows using the provided screenshot as layout reference. Include path fields for ComicRack Source, Remote Sync Target, and Fansadox Source; list ZIP/CBZ files with selectable rows and CBZ, Info, ComicInfo, ENGLISH, and Synced status columns; persist field values and per-entry status/selection; store archive status in the ComicRack Source folder; add Rescan, Select All, Select None, Help, hover instructions, and keep commits going.

Actions:
- Inspected repository structure and existing ZiptoCBZ, TranslateEXGallery, and InfotoComicInfoxml utilities.
- Added shared archive scanning, state persistence, app settings persistence, selection updating, and sync-copy helpers in comicrack_master.py.
- Added master_ui.py with a Tkinter interface inspired by the provided screenshot, including path fields, status table, utility action buttons, rescan, select all/none, help, hover tooltips, and a run log.
- Updated README.md with the master UI entry point and persistence behavior.
- Added unittest coverage for ZIP-first ordering, archive status detection, synced detection, default/persisted selection, and settings round trip.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py`; both passed.
- Created checkpoint commit `3c10218` for the initial master UI implementation.

2026-08-30

Prompt:
Make sure that subdirectories within the source directory are ignored.

Actions:
- Updated archive scanning to inspect only files directly inside ComicRack Source.
- Updated UI help/tooltip text and README wording to state that subdirectories are ignored.
- Added regression coverage proving archives in nested folders are not listed.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py`; both passed.
- Created checkpoint commit for the direct-source-only scanner update.

2026-08-30

Prompt:
When first booted the UI does not load any elements during initial operations. Bring up the UI as a priority, then add a loading bar at the bottom that runs if anything is in progress.

Actions:
- Refactored startup Rescan and utility actions to run through a background worker instead of blocking the Tkinter event loop.
- Added a bottom indeterminate progress bar and status strip behavior for active scan, conversion, translation, and sync operations.
- Disabled action buttons while background work is running to prevent overlapping operations.
- Updated README.md to mention background work and the bottom progress bar.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py`; both passed.

2026-08-30

Prompt:
Add a button at the bottom that opens another window displaying all currently logged comics in a window where they can be copied in list form.

Actions:
- Added a bottom Comic List button that opens a separate Logged Comics window.
- Added a plain text list of currently loaded archive rows with scrollbars, normal selection/copy support, and a Copy All button.
- Added a shared records_as_copy_list helper and regression coverage for the copied list format.
- Updated README.md to document the Comic List button.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py`; both passed.

2026-08-30

Prompt:
Add a remove duplicates functionality, with code stored in a subfolder of the repo, that does a hash comparison between suspected duplicates and moves them to a directory called _DUPLICATES within the source directory along with logging those that have been moved.

Actions:
- Added RemoveDuplicates/remove_duplicates.py with direct-source ZIP/CBZ duplicate detection using same-size grouping plus SHA-256 verification.
- Moves duplicate archives into _DUPLICATES and appends moved-file details to _DUPLICATES/duplicates.log.
- Added RemoveDuplicates README documentation.
- Added a master UI Remove Dups button that runs the utility in the background and rescans afterward.
- Added tests for duplicate moves, CBZ-over-ZIP keep preference, hash mismatch safety, logging, and subdirectory ignoring.
- Added _DUPLICATES to .gitignore so moved local archives are not accidentally staged.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py RemoveDuplicates\\remove_duplicates.py`; both passed.

2026-08-30

Prompt:
Make the entries sortable based on the column clicked.

Actions:
- Added a shared sorted_archive_records helper for sorting archive records by table columns.
- Wired table headings so clicking any column sorts by that column and clicking it again reverses direction.
- Added heading markers to show the active sort direction.
- Kept the default ZIP-first scan order until the user selects a sort column.
- Updated README.md and added regression coverage for column sorting.
- Ran `python -m unittest discover -s tests` and `python -m py_compile comicrack_master.py master_ui.py RemoveDuplicates\\remove_duplicates.py`; both passed.
